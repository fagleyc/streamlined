# Streamlined development / build dependencies
# Install after requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

# Build tool for standalone executable
pyinstaller>=6.0

# Test runner for tests/ (QT_QPA_PLATFORM=offscreen for the GUI tests)
pytest>=8.0
